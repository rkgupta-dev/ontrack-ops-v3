# Live mode — `npm run build` / `npm run deploy:live` (ontrack-operations.web.app).
# Loaded on top of `.env`, so a live build always talks to the production
# backend even while `.env` points `npm run dev` at a local one.
VITE_API_BASE_V2=https://glacier.on-track.in/api
