# Staging mode — `npm run dev:staging` / `npm run build:staging`.
# Vite loads `.env` first, then this file on top, so only the backend URL is
# overridden here; every other key (Maps, Firebase) comes from `.env`.
VITE_API_BASE_V2=https://staging-api.on-track.in/api
